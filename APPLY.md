# 阶段十九：记录卖出 + 持仓 UI

## 系统如何知道你卖出？

默认**不知道**券商账户里的成交。只有以下方式会更新 `config/holdings.db`：

1. **💸 记录卖出**（本包新增）— 选代码、填数量、确认
2. **📋 粘贴成交** — 粘贴券商成交文本自动识别买卖
3. **编辑持仓** 改数量，或 **删除持仓**
4. macOS **trade_monitor** 监听通知（可选、有门槛）

## 覆盖后

```bash
python deploy/ctl.py restart-all
# 打开 8502 → 持仓与卖出区间 → 「记录卖出」
```
