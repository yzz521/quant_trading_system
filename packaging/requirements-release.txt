# 便携包专用依赖（与 requirements.txt 一致，但不含 -e .，
# 打包时直接把依赖装进独立 Python 运行时，无需 editable 安装）

# Core (required)
pandas>=2.0
numpy>=1.24
matplotlib>=3.7
pyarrow>=12.0          # parquet cache backend
pyyaml>=6.0
requests>=2.28         # notifier / 推送通道（email、Server酱、飞书）

# Data sources
akshare>=1.10          # A股 / 港股 / 期货
yfinance>=0.2.30       # 美股 / 黄金 / ETF

# Machine learning (MLStrategy)
scikit-learn>=1.3

# Dashboard
streamlit>=1.28
